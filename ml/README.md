# FraudShield AI - ML Module (Phase 1 scaffolding)

This directory is reserved for the fraud scoring model and inference service.

What will live here:

- Training notebooks/scripts and feature pipelines
- A versioned model artifact
- An inference API (FastAPI) that the Node backend can call for risk scores + explanations

For now (Phase 1), this is just scaffolding so the repo structure matches the target architecture.

