# infra/

This folder contains infrastructure and deployment artifacts.

Status:

- Local dev uses `backend/docker-compose.yml` for Postgres + Redis.
- CI/CD and production deploy manifests will live here in later phases.

